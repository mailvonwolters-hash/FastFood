Update the OCR relevance filter component to strictly process ONLY the following explicitly defined domain values and ignore everything else:

1. ALLOWED DATA MATRIX & CODES (Strict Whitelist):
   - Customer Identifiers: Name, Address, Stop Sequence.
   - Portion Quantities: "1", "2", "1/2", "1 1/2".
   - Packaging Rules: "Alu", "kein Alu".
   - Dessert / Extra Items: "Joghurt", "Obst", "KN" (Kein Nachtisch), "kein Pudding".
   - Menu Plan (Speiseplan) Status:
     - "SP" = Customer needs new menu plan.
     - "kein SP" = Customer does NOT want menu plan (orders via phone).
   - Delivery Absence / Status: "KH" (Krankenhaus), "Keine Belieferung", "Storno".
   - Allowed Action Keywords ONLY: "Klingeln", "warten", "schlüssel".

2. STRICT NOISE FILTER (Ignore Rule):
   - DISCARD ANY TEXT, note, or scribble that does not match the items above.
   - Ignore all internal calculations, payment notes, birthday reminders, or unlisted freeform handwritten text.

3. Data Output:
   - Map only verified whitelisted entries into the final customer delivery profile.
